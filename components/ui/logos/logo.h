#ifndef LOGO_H
#define LOGO_H

#include "lvgl.h"

// Declare the image descriptor as external since it's defined in logo.c
extern const lv_img_dsc_t logo;  // Note: Use whatever variable name the converter generated

#endif // LOGO_H